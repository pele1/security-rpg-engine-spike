# Keep generated PNGs at normal repository paths while representing them as
# reviewable text pointers in Git and GitHub pull requests.
shared/assets/generated/**/*.png filter=lfs diff=lfs merge=lfs -text
